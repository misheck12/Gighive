<!DOCTYPE html>
<html>
  <body>
    <h1>Hello <%= @client.name %>,</h1>
    <p>Your task "<strong><%= @task.title %></strong>" has been marked as completed.</p>
    <p>Details:</p>
    <ul>
      <li><strong>Freelancer:</strong> <%= @task.freelancer.name %></li>
      <li><strong>Budget:</strong> <%= humanized_money_with_symbol(@task.budget) %></li>
      <li><strong>Completed On:</strong> <%= @task.updated_at.strftime("%B %d, %Y") %></li>
    </ul>
    <p>You can view the completed task <a href="<%= task_url(@task) %>">here</a>.</p>
    <p>Thank you for using GigHive!</p>
  </body>
</html>