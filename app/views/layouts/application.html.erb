<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>GigHive - Empowering Freelancers</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap 5 CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">


  <!-- FontAwesome -->
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">

  <!-- Application CSS -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: 'all' %>

  <!-- Importmap (if using importmap for JavaScript modules) -->
  <%= javascript_importmap_tags %>

  <!-- Favicon -->
  <%= favicon_link_tag 'GigHive_logo.png' %>
</head>

<body>
  <nav class="navbar navbar-expand-lg" style="background-color: #2A2A2A;">
    <div class="container">
      <%= link_to 'GigHive', root_path, class: 'navbar-brand fw-bold', style: "color: #FFD700;" %>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="color: #FFFFFF;">
                <i class="fa fa-user-circle"></i> <%= current_user.name %>
              </a>
              <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                <li><%= link_to 'Edit Profile', edit_user_registration_path, class: 'dropdown-item' %></li>
                <li><%= link_to 'Dashboard', dashboard_path, class: 'dropdown-item' %></li>
                <li><hr class="dropdown-divider"></li>
                <li><%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %></li>
              </ul>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'Sign in', new_user_session_path, class: 'nav-link', style: "color: #FFFFFF;" %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign up', new_user_registration_path, class: 'nav-link', style: "color: #FFFFFF;" %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>

  <main class="container my-5">
    <%= yield %>
  </main>

  <footer class="text-white py-3 mt-auto" style="background-color: #2A2A2A;">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <p class="mb-0">&copy; <%= Time.now.year %> GigHive. All rights reserved.</p>
        </div>
        <div class="col-md-6 text-md-end">
          <a href="<%= root_path %>" class="text-white me-2">Home</a>
          <a href="#" class="text-white me-2">About Us</a>
          <a href="#" class="text-white me-2">Contact</a>
          <a href="#" class="text-white">Terms of Service</a>
        </div>
      </div>
    </div>
  </footer>

  <!-- Bootstrap 5 JS Bundle (includes Popper) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-..." crossorigin="anonymous" defer></script>
</body>
</html>
